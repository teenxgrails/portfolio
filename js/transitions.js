/* transitions.js — intentionally minimal.
 *
 * Page-to-page navigation is plain: no scale/zoom view-transition, no fade
 * overlay. Links marked [data-transition] are ordinary same-origin anchors —
 * the browser navigates directly. This keeps the site still and editorial
 * (ERD / Balenciaga direction) rather than animated.
 *
 * Kept as a no-op file so existing <script> tags and markup don't need edits.
 */
